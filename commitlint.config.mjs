// Commit messages follow Conventional Commits and are written in Portuguese,
// matching the CipherMesh repository. Only the structure is enforced here.
const config = { extends: ["@commitlint/config-conventional"] };

export default config;
